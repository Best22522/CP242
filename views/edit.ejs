<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Note</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    /* Gradient background */
    body {
      background: linear-gradient(to left, #4E1F00, #74512D, #FEBA17 , #F8F4E1);
    }
  </style>
</head>
<body class="bg-gray-100">


  <div class="max-w-4xl mx-auto p-4 mt-10 bg-white rounded-lg shadow-md">
    <h1 class="text-2xl font-bold mb-4">Edit Note</h1>

    <form action="/notes/edit/<%= note._id %>" method="POST">
      <div class="mb-4">
        <label for="title" class="block text-gray-700">Title</label>
        <input type="text" id="title" name="title" value="<%= note.title %>" class="mt-2 p-2 w-full border border-gray-300 rounded-md">
      </div>

      <div class="mb-4">
        <label for="content" class="block text-gray-700">Content</label>
        <textarea id="content" name="content" rows="4" class="mt-2 p-2 w-full border border-gray-300 rounded-md"><%= note.content %></textarea>
      </div>

      <button type="submit" class="bg-blue-500 text-white p-2 rounded-md">Save Changes</button>
    </form>
  </div>

</body>
</html>
